#include <iostream>
using namespace std;
int main(){
    int index = 0;

    // Syntax:
    // while(condition)
    // {
    //     C++ statements;
    // }
    
    while(index<12)
    {
        cout<<"We are at index number "<<index<<endl;
        index = index + 1;
    }
    return 0;
}